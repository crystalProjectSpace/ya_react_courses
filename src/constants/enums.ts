export enum INGREDIENT_TYPE {
    BUN = 'bun',
    SAUCE = 'sauce',
    MAIN = 'main'
}

export enum OrderStatus {
    CREATED = 'created',
    PENDING = 'pending',
    DONE = 'done',
    CANCELLED = 'cancelled'
}
